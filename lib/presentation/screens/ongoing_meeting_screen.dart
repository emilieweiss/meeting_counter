import 'package:flutter/material.dart';

class OngoingMeetingScreen extends StatelessWidget {
  const OngoingMeetingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("On going meeting"));
  }
}
